# Roster Functional + Responsive Plan

## Current State
- UI ready (Calendar, Shifts, Swaps, Availability)
- Missing DB: `shifts`, `roster_entries` tables
- Responsive table exists (scroll/horizontal)

## Steps
1. ✅ Create supabase/migrations/roster_schema.sql (shifts, roster_entries tables)
2. Create supabase/migrations/seed_roster.sql (sample shifts/entries)
3. Test /roster → Calendar (no errors)
4. Responsive: Mobile calendar/table (grid/stack)
5. Features: Drag-drop, publish roster, swap notifications

**User: Confirm plan → Proceed?**
